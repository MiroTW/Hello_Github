name: Python package

on:
  release:
    types: [created]
  push:
    branches: master
    paths-ignore:
      - '.github/**' 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-python@v1
      with:
        python-version: 3.7
    - name: Test with pytest
      run: |
        pip install pytest
        pytest
